# Single source of truth for the release version. Bump this in the same commit
# that moves the git tag, so an image, a checkout and a running container can
# all be asked the same question and agree.
__version__ = "1.0.0"
